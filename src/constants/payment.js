/** sessionStorage key: event id being paid for, so /result can redirect after Paymob redirect. */
export const PAYMENT_RETURN_EVENT_ID_KEY = "venuva_last_payment_event_id";
/** sessionStorage key: return path after payment success. */
export const PAYMENT_RETURN_PATH_KEY = "venuva_last_payment_return_path";
